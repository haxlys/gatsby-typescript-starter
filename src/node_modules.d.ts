// vscode ts파일에서 global variable 에러표시 나지 않도록 설정
declare const __PATH_PREFIX__: string

// ts 파일에서는 module을 declare 파일(lib.d.ts)에서 찾는데
// 해당 라이브러리가 declare 파일이 없다면 vscode에서 에러밑줄을 표시한다(없는 경우는 typescript 버전으로 작성되지 않은 라이브러리)
// react-helmet은 @type/react-helmet이 없기 때문에 에러 표시 나지 않도록 설정해줌
declare module 'react-helmet'
